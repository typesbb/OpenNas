<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OpenNas.Views.ConnectionSettingsPage"
             Title="连接设置"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False"
             BackgroundColor="{AppThemeBinding Light={StaticResource AppBackground}, Dark={StaticResource AppBackgroundDark}}">
    <Grid RowDefinitions="Auto,*">
        <Grid Grid.Row="0"
              Padding="4,8,8,4"
              ColumnDefinitions="Auto,*,Auto"
              ColumnSpacing="4">
            <Button Grid.Column="0"
                    Text="‹"
                    FontSize="28"
                    Style="{StaticResource ToolbarIconButton}"
                    SemanticProperties.Hint="返回"
                    Clicked="OnBackClicked" />
            <Label Grid.Column="1"
                   Text="连接设置"
                   FontFamily="OpenSansSemibold"
                   FontSize="18"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
        </Grid>
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="16" Spacing="16">

            <Label Text="NAS 地址" Style="{StaticResource SectionTitle}" />

            <!-- 内网 (LAN) -->
            <Border Style="{StaticResource CardBorder}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="内网 (LAN)" FontAttributes="Bold" FontSize="15" />
                    <Label Text="在家或与 NAS 同一网络时使用" Style="{StaticResource MicroCaption}" Margin="0,-4,0,0" />

                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="0">
                        <Picker x:Name="LanProtocol">
                            <Picker.Items><x:String>https://</x:String><x:String>http://</x:String></Picker.Items>
                        </Picker>
                        <Entry x:Name="LanHost" Grid.Column="1" Placeholder="IP 地址或域名" Keyboard="Url" />
                        <Entry x:Name="LanPort" Grid.Column="2" Placeholder="端口" Text="5001" Keyboard="Numeric" HorizontalTextAlignment="End" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- 外网 (WAN) -->
            <Border Style="{StaticResource CardBorder}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="外网 (WAN)" FontAttributes="Bold" FontSize="15" />
                    <Label Text="在外出或不同网络时使用（需要 QuickConnect 或 DDNS）" Style="{StaticResource MicroCaption}" Margin="0,-4,0,0" />

                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="0">
                        <Picker x:Name="WanProtocol">
                            <Picker.Items><x:String>https://</x:String><x:String>http://</x:String></Picker.Items>
                        </Picker>
                        <Entry x:Name="WanHost" Grid.Column="1" Placeholder="xxx.cn5.quickconnect.cn" Keyboard="Url" />
                        <Entry x:Name="WanPort" Grid.Column="2" Placeholder="端口" Text="5001" Keyboard="Numeric" HorizontalTextAlignment="End" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Auto-switch toggle -->
            <Border Style="{StaticResource CardBorder}" Padding="14">
                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                    <Switch x:Name="AutoSwitchToggle" Toggled="OnAutoSwitchToggled" VerticalOptions="Center" WidthRequest="52" HorizontalOptions="Start" />
                    <Label Grid.Column="1" Text="自动切换内/外网连接" VerticalOptions="Center" FontSize="15" />
                </Grid>
            </Border>

            <Button Text="保存" Style="{StaticResource PrimaryButton}" Clicked="OnSaveClicked" />

            <Label x:Name="StatusLabel" Style="{StaticResource Caption}" HorizontalOptions="Center" Margin="0,0,0,0" />
        </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>

